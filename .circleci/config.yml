version: 2.1
jobs:
  build:
    environment:
      CIRCLE_TRIGGER_SOURCE: << pipeline.trigger_source >>
      CIRCLE_WORKFLOW_ID: ${CIRCLE_WORKFLOW_ID}
    docker:
      - image: 218546966473.dkr.ecr.us-east-1.amazonaws.com/circle-ci:stitch-tap-tester-uv
    steps:
      - checkout
      - run:
          name: 'Setup virtual env'
          command: |
            uv venv --python 3.12 /usr/local/share/virtualenvs/tap-harvest
            source /usr/local/share/virtualenvs/tap-harvest/bin/activate
            uv pip install .
            uv pip install pylint
      - run:
          name: 'JSON Validator'
          command: |
            source /usr/local/share/virtualenvs/tap-tester/bin/activate
            stitch-validate-json tap_harvest/schemas/*.json
      - run:
          name: 'pylint'
          command: |
            source /usr/local/share/virtualenvs/tap-harvest/bin/activate
            uv pip install pylint
            pylint tap_harvest -d C,R,W
      - add_ssh_keys
      - run:
          name: 'Unit Tests'
          command: |
            source /usr/local/share/virtualenvs/tap-harvest/bin/activate
            uv pip install pytest coverage parameterized
            coverage run --source tap_harvest -m pytest tests/unittests
      - run:
          name: 'Coverage Report'
          command: |
            source /usr/local/share/virtualenvs/tap-harvest/bin/activate
            coverage report -m --fail-under=100
            coverage html
      - store_test_results:
          path: test_output/report.xml
      - store_artifacts:
          path: htmlcov
      - add_ssh_keys
      - run:
          name: 'Integration Tests'
          no_output_timeout: 30m
          command: |
            source /usr/local/share/virtualenvs/tap-tester/bin/activate
            uv pip install --upgrade awscli
            aws s3 cp s3://com-stitchdata-dev-deployment-assets/environments/tap-tester/tap_tester_sandbox dev_env.sh
            source dev_env.sh
            unset USE_STITCH_BACKEND
            run-test --tap=tap-harvest tests

workflows:
  version: 2
  commit:
    jobs:
      - build:
          context: circleci-user
  build_daily:
    triggers:
      - schedule:
          cron: "0 0 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - build:
          context: circleci-user
